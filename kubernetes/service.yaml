apiVersion: v1
kind: Service
metadata:
  name: financeapp-service
  namespace: finance
spec:
  selector:
    app: financeapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: NodePort
