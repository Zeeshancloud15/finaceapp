apiVersion: apps/v1
kind: Deployment
metadata:
  name: financeapp
  namespace: financeapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: financeapp
  template:
    metadata:
      labels:
        app: financeapp
    spec:
      containers:
      - name: financeapp
        image: financeapp:latest
        ports:
        - containerPort: 3000
